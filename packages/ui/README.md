# V2 UI Package

Shared accessible primitives will be extracted here only after their current class contracts and visual regression coverage are known. The package may contain layout, form, status, table, dialog, and evidence components. It must not contain tenant authorization, formula math, inventory mutation, or provider-specific logic.
