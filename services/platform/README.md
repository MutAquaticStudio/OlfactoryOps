# Platform Service Boundary

Tenant lifecycle, authentication/session, members, security, billing controls, and observability. Authorization is server-side and tenant-scoped. Existing platform behavior remains in `server/src/` and `worker/` until parity tests approve extraction.
