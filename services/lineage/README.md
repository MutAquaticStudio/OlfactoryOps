# Lineage Service Boundary

Append-only domain events, audit evidence, provenance references, correlation, and idempotency records. Events are tenant-scoped, versioned, and written in the same transaction as the domain mutation.
