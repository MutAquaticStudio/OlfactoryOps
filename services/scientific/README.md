# Scientific Service Boundary

The boundary for Python/C++ scientific adapters. V2 TypeScript contracts describe jobs, artifacts, features, embeddings, predictions, similarity, and explainability. No engine or model is implemented in Phase 0. Domain services consume evidence through the API only.
