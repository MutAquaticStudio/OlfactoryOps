# Scientific Prediction Boundary

Prediction results must include bounded output, evidence status, uncertainty when available, and provenance. Predictions are advisory and never replace deterministic domain gates.
