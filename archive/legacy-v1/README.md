# Archived V1 source

This directory contains source modules removed from the active pre-V2 product. It is retained for historical reference only and is not imported by the frontend, local API, Worker routes, scheduled jobs, or production build.

The active product keeps shared infrastructure such as tenant isolation, mutation idempotency, inventory ledger, audit, authentication, and document evidence. V2 implementation has not started.
