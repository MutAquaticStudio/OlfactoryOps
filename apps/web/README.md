# V2 Web Boundary

This is the target boundary for the React/Vite web application. The current implementation remains in `src/` during the transition. Route guards, tenant context, permission projection, API client behavior, and accessibility tests must pass before a feature moves here. Do not duplicate business rules in the browser.
