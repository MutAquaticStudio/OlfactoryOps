# Vector Infrastructure Boundary

Vector indexes store derived embeddings with tenant, source, version, and revocation metadata. PostgreSQL re-checks authorization and source state after vector search. No index is provisioned in Phase 0.
