# Object Storage Boundary

Private documents and scientific artifacts use a storage adapter. Ownership, checksums, lifecycle, and access metadata remain in PostgreSQL. Signed URLs are never a source of RAG authorization.
