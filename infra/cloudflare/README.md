# Cloudflare Infrastructure Boundary

Cloudflare Pages, Workers, D1 edge/control plane, tenant router, queues, SaaS custom domains, and Workers AI/Vectorize bindings belong here. Cloudflare is not the V2 authoritative database writer. Credentials and deploy configuration are deferred; Phase 0 performs no deployment.
