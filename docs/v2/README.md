# OlfactoryOps V2 scope hand-off

V2 implementation has not started. This directory records only the post-cleanup hand-off and Scope Lock V0.4 decisions.

The pre-V2 cleanup must finish with `CLEANUP_VERIFICATION.md` in status `PASS` before scientific-core, Osmo, new formula architecture or new global material data is introduced.
